[pytest]
markers =
    smoke: critical smoke tests for CI
    regression: full regression suite
    performance: performance-related tests
    mocked: tests using mocked HTTP calls

addopts =
    --strict-markers
